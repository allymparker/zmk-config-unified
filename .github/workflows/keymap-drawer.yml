# Example for using the keymap-drawer ZMK user config workflow
name: Draw ZMK keymaps
on:
  workflow_dispatch:
  push:
    paths:
      - "base/*"
      - "config_*/*.keymap"
      - "config_*/*.dtsi"
      - "keymap-drawer/config.yaml"
      # - 'config/boards/*/*/*.keymap'

jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    permissions:
      contents: write # allow workflow to commit to the repo
    with:
      keymap_patterns: "config_*/*.keymap" # path to the keymaps to parse
      config_path: "keymap-drawer/config.yaml" # config file, ignored if not exists
      output_folder: "keymap-drawer" # path to save produced SVG and keymap YAML files
      # amend_commit: true
      draw_args: "corneish_zen:'--ghost-keys 20 24 25 29 30 35' corneroll:'-k corne'"
